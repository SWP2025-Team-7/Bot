from enum import Enum

class Messages(Enum):
    LANGUAGE = "language"
    LANGUAGE_CHANGED = "language_changed"
    ERROR = "error"
    LOGIN = "login"
    CANCEL = "cancel"
    INSTRUCTIONS = "instructions"
    SEND = "send"
    DATA = "data"
